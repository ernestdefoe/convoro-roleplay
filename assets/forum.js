// convoro-roleplay — forum runtime.
// The post-as-character byline is handled server-side via the core PostIdentity
// hook, so no client work is needed to display characters. The composer "post
// as" selector + word counter register here (next phase).
